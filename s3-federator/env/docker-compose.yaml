name: ceeds-s3-federator

services:
  postgres:
    image: postgres:17
    ports:
      - "5440:5432"
    environment:
      POSTGRES_DB: ${S3_FEDERATOR_DB_NAME}
      POSTGRES_USER: ${S3_FEDERATOR_DB_USERNAME}
      POSTGRES_PASSWORD: ${S3_FEDERATOR_DB_PASSWORD}
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "sh -c 'pg_isready -U ${S3_FEDERATOR_DB_USERNAME} -d ${S3_FEDERATOR_DB_NAME}'" ]
      interval: 10s
      timeout: 3s
      retries: 3

volumes:
  postgres_data:
